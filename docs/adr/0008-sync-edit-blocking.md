# 同期中の編集ブロック（同期オーバーレイ）

同期（明示・初期）の実行中は、Vault 画面全体を同期オーバーレイで覆い編集をブロックする。同期はプル/プッシュ両方向で Note を上書きしうるため、実行中の編集を受け入れると同期結果と競合する。代替案として「同期をバックグラウンドで実行し編集は継続、完了後にツリー・索引のみ差し替える」があったが、MVP では一貫性の保証と実装の単純さを優先しブロックを選んだ。オーバーレイは同期 API の完了時点で解除し、後続の画面再読み込み（ツリー・索引の再取得）は待たない。