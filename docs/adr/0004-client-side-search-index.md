# 全文検索の索引はクライアント側でメモリ展開する

全文検索は全 Note をブラウザのメモリに読み込み、FlexSearch / MiniSearch で索引化する。個人 Vault の規模（数百〜数千 Note）では瞬時に完了し、サーバー側の索引構築・同期処理が不要になる。クイックスイッチャーやバックリンク走査も同一のメモリ展開を再利用する。

## Consequences

- 数万ファイル規模のリポジトリには耐えない。その時はサーバー側索引への移行を再検討する（個人ツール前提では不要と判断）
