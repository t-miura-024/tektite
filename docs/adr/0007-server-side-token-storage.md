# サーバー側トークン保持 (KV)

Cron 同期はユーザー Cookie を持てないため、GitHub OAuth のアクセストークンとリフレッシュトークンを KV に AES-GCM 暗号化して保存し、期限切れ時はリフレッシュトークンで自動延長する方式を採用する。ログイン callback 時に保存し、同期対象 Vault の write 権限は保存時に確認する。ADR-0002 の「トークンは Cookie のみ、サーバー側ストレージゼロ」を拡張する。
