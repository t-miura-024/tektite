# ステートレス認証: GitHub トークンを暗号化 Cookie に格納する

Workers プロキシのセッション管理として、GitHub トークンを Workers シークレット由来の鍵で AES-GCM 暗号化し Cookie に格納する。サーバー側ストレージ（KV / D1）は一切使わない。個人ツールではサーバー側での即時失効の必要性が低く、無料枠内でストレージゼロの構成を優先した。

## Considered Options

- KV セッションストア: サーバー側ログアウトが可能だが、リクエストごとに読取クォータを消費する
- D1: 個人ツールには過剰な構成
