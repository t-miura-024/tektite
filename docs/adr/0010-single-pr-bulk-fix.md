# 既存違反への一括修正戦略（単一巨大PR）

提示19観点＋デファクト厳密ルールを最初から `error` で導入し、既存違反を単一巨大PRで一括修正する。linter指摘が0になるまでひたすら修正を繰り返し、機械的な0達成をもって完了とする。内容の厳密レビューは行わない。技術的負債を温存しないことを優先した。代替案として連番PR（レビュー粒度は良いが本計画では厳密レビュー不要のため効率を優先して不採用）、warn段階的昇格・grandfather（恒久的二重基準を残すため不採用）を検討した。
