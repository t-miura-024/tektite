pre-commit:
  jobs:
    - name: format
      glob: '*.{ts,tsx,js,jsx,json,css,html}'
      run: pnpm exec oxfmt {staged_files}
      stage_fixed: true
    - name: lint
      run: pnpm lint

pre-push:
  jobs:
    - name: test
      run: pnpm test
