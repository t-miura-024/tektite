# src/application — アプリケーション層

ユースケースを進行させる層。

## 置くもの

- ユースケース（例: ログイン、Vault 一覧取得、Note 保存）
- 外部サービス向けのポート（インターフェース）定義

## 依存規則

- `src/domain` には依存してよい
- `src/infra` の具体実装は import しない（依存性逆転: ポートはここで定義し、実装は UI 層の組成ルートから注入する）
- React コンポーネントは置かない（→ `src/ui`）
